import { colors } from "./colors";
const defaultTheme = {
  primary: colors.purple[400],
  primaryColorHover: colors.purple[500],
  primaryColorActive: colors.purple[900],
  textColor: colors.gray[100],
};

export { defaultTheme };
